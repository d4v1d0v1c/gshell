package main

/*
VAR int ret,freq, int, vpid, ppid, apid
VAR string para, amode
MAIN
    para <- getenv "#1"
    if para=""
      print "Frequency missing"
      freq <- 626000
    else
      freq <- integer para
    endif
END
*/

func main() {
}
